
//: Declare String Begin

/*: "getDeviceID" :*/
fileprivate let user_privacyName:String = "main indicator objectgetDevi"
fileprivate let notiFieldText:[Character] = ["c","e","I","D"]

/*: "getFirebaseID" :*/
fileprivate let appPhoneData:[Character] = ["g","e"]
fileprivate let show_totalFormat:String = "phone searchtFir"

/*: "getAreaISO" :*/
fileprivate let userTunData:String = "click carrier levelgetAr"
fileprivate let user_carrierValue:[Character] = ["O"]

/*: "getProxyStatus" :*/
fileprivate let app_appearTitle:String = "type languagegetPro"
fileprivate let appAlwaysUrl:String = "float skin evaluate s representxyStatus"

/*: "getMicStatus" :*/
fileprivate let data_protectionFormat:[Character] = ["g","e"]
fileprivate let k_buildPath:String = "arrow kittMicS"

/*: "getPhotoStatus" :*/
fileprivate let show_receiveTitle:String = "string ok datagetPho"
fileprivate let constScreenStr:String = "tdisappear"

/*: "getCameraStatus" :*/
fileprivate let showSelectValue:String = "body domain can floatgetCam"

/*: "reportAdjust" :*/
fileprivate let userHeadFormat:String = "apppo"
fileprivate let user_phoneData:String = "textst"

/*: "requestLocalPush" :*/
fileprivate let k_globalMessage:[Character] = ["r","e","q","u","e","s","t","L","o"]
fileprivate let kTitleStr:[Character] = ["c","a","l","P","u","s","h"]

/*: "getLangCode" :*/
fileprivate let mainArrayWarnKey:[Character] = ["g","e","t","L","a","n"]
fileprivate let mainMicFormat:String = "application shared color enter titlegCode"

/*: "getTimeZone" :*/
fileprivate let user_mainId:String = "hide"
fileprivate let main_thatPath:[Character] = ["t","T","i","m","e","Z","o","n","e"]

/*: "getInstalledApps" :*/
fileprivate let showRecordFormat:[Character] = ["g","e","t","I","n","s","t","a","l"]
fileprivate let kNameMsg:[Character] = ["l","e","d","A","p","p","s"]

/*: "getSystemUUID" :*/
fileprivate let const_headName:String = "getSysresponse log level policy skin"
fileprivate let app_reduceText:String = "temUUIDcurrent frame poor"

/*: "getCountryCode" :*/
fileprivate let kPageKey:String = "region policy kind addgetCou"
fileprivate let appColorStr:String = "ntryCodevertical grant"

/*: "inAppRating" :*/
fileprivate let main_ratingUrl:String = "inAppRsuccess hand disk"
fileprivate let data_groupUrl:[Character] = ["a","t","i","n","g"]

/*: "apPay" :*/
fileprivate let userSourceUrl:String = "file raw will arrayapPay"

/*: "subscribe" :*/
fileprivate let constAppId:String = "suafter"
fileprivate let const_responseMsg:[Character] = ["s","c","r","i","b","e"]

/*: "openSystemBrowser" :*/
fileprivate let userMethodKey:String = "temp contactopenSy"
fileprivate let mainRunData:[Character] = ["s","e","r"]

/*: "closeWebview" :*/
fileprivate let mainStateUrl:[Character] = ["c","l","o","s","e","W","e","b","v","i","e","w"]

/*: "openNewWebview" :*/
fileprivate let dataDeviceContent:String = "oremoven"
fileprivate let main_sceneKey:String = "trust size trigger stateNewWeb"

/*: "reloadWebview" :*/
fileprivate let userMicTitle:String = "RELOA"
fileprivate let noti_largePanelContent:String = "max indicator message builddWebview"

/*: "typeName" :*/
fileprivate let appNetworkUrl:[UInt8] = [0xfb,0xf6,0xff,0xea,0xc1,0xee,0xe2,0xea]

private func trackFlexible(name num: UInt8) -> UInt8 {
    return num ^ 143
}

/*: "deviceID" :*/
fileprivate let const_logName:[UInt8] = [0x6a,0x6b,0x78,0x67,0x6d,0x6b,0x47,0x4a]

private func statuteTitle(operation num: UInt8) -> UInt8 {
    return num ^ 14
}

/*: "fireBaseID" :*/
fileprivate let data_cornerAlbumFormat:[UInt8] = [0x98,0x97,0x8c,0x9b,0xbc,0x9f,0x8d,0x9b,0xb7,0xba]

private func spaceLine(black num: UInt8) -> UInt8 {
    return num ^ 254
}

/*: "areaISO" :*/
fileprivate let noti_logSharedPath:[UInt8] = [0x39,0x2a,0x3d,0x39,0x11,0xb,0x17]

/*: "isProxy" :*/
fileprivate let noti_environmentPath:[UInt8] = [0x87,0x91,0x6e,0x90,0x8d,0x96,0x97]

fileprivate func titleTrackBody(point num: UInt8) -> UInt8 {
    let value = Int(num) + 226
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "langCode" :*/
fileprivate let user_sessionRegionValue:[UInt8] = [0x5c,0x51,0x5e,0x57,0x73,0x5f,0x54,0x55]

/*: "timeZone" :*/
fileprivate let show_decideTitle:[UInt8] = [0x2b,0x36,0x32,0x3a,0x5,0x30,0x31,0x3a]

private func sizeInfo(tap num: UInt8) -> UInt8 {
    return num ^ 95
}

/*: "installedApps" :*/
fileprivate let app_bridgeRevenueKey:[UInt8] = [0xfe,0xf9,0xe4,0xe3,0xf6,0xfb,0xfb,0xf2,0xf3,0xd6,0xe7,0xe7,0xe4]

private func toNumber(permission num: UInt8) -> UInt8 {
    return num ^ 151
}

/*: "systemUUID" :*/
fileprivate let app_reduceValue:[UInt8] = [0x6,0xc,0x6,0x1,0x10,0x18,0x20,0x20,0x3c,0x31]

/*: "countryCode" :*/
fileprivate let showIndexLinkToStr:[UInt8] = [0xc7,0xcb,0xd1,0xca,0xd0,0xd6,0xdd,0xe7,0xcb,0xc0,0xc1]

private func presentationRatingActive(title num: UInt8) -> UInt8 {
    return num ^ 164
}

/*: "status" :*/
fileprivate let const_forwardUrl:[UInt8] = [0x29,0x2e,0x3b,0x2e,0x2f,0x29]

/*: "isAuth" :*/
fileprivate let noti_takeTitle:[UInt8] = [0xe6,0xfc,0xce,0xfa,0xfb,0xe7]

/*: "isFirst" :*/
fileprivate let userCanArrayStr:[UInt8] = [0x74,0x73,0x72,0x69,0x46,0x73,0x69]

/*: __LocalPush" :*/
fileprivate let kPlatformPath:String = "after adjust mask previous us__LocalPu"
fileprivate let main_panelUpgradeKey:String = "dateh"

/*: "identifier" :*/
fileprivate let k_mirrorUrl:[UInt8] = [0xe7,0xea,0xeb,0xe0,0xfa,0xe7,0xe8,0xe7,0xeb,0xfc]

/*: "HTTPProxy" :*/
fileprivate let constWithFileId:[Character] = ["H","T","T","P"]
fileprivate let noti_presentationId:[Character] = ["P","r","o","x","y"]

/*: "HTTPSProxy" :*/
fileprivate let show_currentMsg:String = "HTTPpoor"
fileprivate let show_versionTitle:[Character] = ["P","r","o","x","y"]

/*: "SOCKSProxy" :*/
fileprivate let user_countryData:String = "SOCdevice"
fileprivate let showUsId:[Character] = ["y"]

/*: "__SCOPED__" :*/
fileprivate let show_grantStr:String = "__SCOmirror camera application application"

/*: "tap" :*/
fileprivate let data_fromProgressFormat:String = "verticalap"

/*: "tun" :*/
fileprivate let showPageStr:String = "createun"

/*: "ipsec" :*/
fileprivate let app_selectedMsg:String = "IPSEC"

/*: "ppp" :*/
fileprivate let app_modelPath:String = "PPP"

/*: "Retry After or Go to " :*/
fileprivate let k_protectionValue:String = "failure deviceRetr"
fileprivate let appCaptureRatingData:String = "make currency iter or G"
fileprivate let k_topTitle:[Character] = ["o"," ","t","o"," "]

/*: "Feedback" :*/
fileprivate let user_warnClickRequestName:String = "carrier as deadline caseFeedbac"
fileprivate let k_sinceTitle:String = "K"

/*: " to contact us" :*/
fileprivate let dataArrayName:String = " to margin decide again network"
fileprivate let dataMessageCoreMsg:[Character] = [" ","u","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  OldFutureDuration.swift
//  OverseaH5
//
//  Created by young on 2025/9/23.
//

//: import CoreTelephony
import CoreTelephony
//: import FirebaseMessaging
import FirebaseMessaging
//: import HandyJSON
import HandyJSON
//: import StoreKit
import StoreKit
//: import UIKit
import UIKit

/// H5事件
//: private let getDeviceID     = "getDeviceID"        // 获取设备号
private let dataTitleViewId = (String(user_privacyName.suffix(7)) + String(notiFieldText)) // 获取设备号
//: private let getFirebaseID   = "getFirebaseID"      // 获取FireBaseToken
private let k_objectOperationMessage = (String(appPhoneData) + String(show_totalFormat.suffix(4)) + "ebaseID") // 获取FireBaseToken
//: private let getAreaISO      = "getAreaISO"         // 获取 SIM/运营商 地区ISO
private let notiNetKey = (String(userTunData.suffix(5)) + "eaIS" + String(user_carrierValue)) // 获取 SIM/运营商 地区ISO
//: private let getProxyStatus  = "getProxyStatus"     // 获取是否使用了代理
private let constGroupWarnMessage = (String(app_appearTitle.suffix(6)) + String(appAlwaysUrl.suffix(8))) // 获取是否使用了代理
//: private let getMicStatus    = "getMicStatus"       // 获取麦克风权限
private let showDismissKey = (String(data_protectionFormat) + String(k_buildPath.suffix(5)) + "tatus") // 获取麦克风权限
//: private let getPhotoStatus  = "getPhotoStatus"     // 获取相册权限
private let constMonitorText = (String(show_receiveTitle.suffix(6)) + "toSta" + constScreenStr.replacingOccurrences(of: "disappear", with: "us")) // 获取相册权限
//: private let getCameraStatus = "getCameraStatus"    // 获取相机权限
private let noti_pushPhotoStr = (String(showSelectValue.suffix(6)) + "eraStatus") // 获取相机权限
//: private let reportAdjust    = "reportAdjust"       // 上报Adjust
private let userPlusName = (userHeadFormat.replacingOccurrences(of: "app", with: "re") + "rtAd" + user_phoneData.replacingOccurrences(of: "text", with: "ju")) // 上报Adjust
//: private let requestLocalPush = "requestLocalPush"  // APP在后台收到音视频通话推送
private let constMaxKey = (String(k_globalMessage) + String(kTitleStr)) // APP在后台收到音视频通话推送
//: private let getLangCode      = "getLangCode"        // 获取系统语言
private let noti_zoneLineMsg = (String(mainArrayWarnKey) + String(mainMicFormat.suffix(5))) // 获取系统语言
//: private let getTimeZone      = "getTimeZone"        // 获取当前系统时区
private let kInstallTotalryContent = (user_mainId.replacingOccurrences(of: "hide", with: "ge") + String(main_thatPath)) // 获取当前系统时区
//: private let getInstalledApps = "getInstalledApps"   // 获取已安装应用列表
private let k_decisionDeviceKey = (String(showRecordFormat) + String(kNameMsg)) // 获取已安装应用列表
//: private let getSystemUUID    = "getSystemUUID"      // 获取系统UUID
private let main_systemId = (String(const_headName.prefix(6)) + String(app_reduceText.prefix(7))) // 获取系统UUID
//: private let getCountryCode   = "getCountryCode"     // 获取当前系统地区
private let app_entityServerKey = (String(kPageKey.suffix(6)) + String(appColorStr.prefix(8))) // 获取当前系统地区
//: private let inAppRating      = "inAppRating"        // 应用内评分
private let noti_descriptionPath = (String(main_ratingUrl.prefix(6)) + String(data_groupUrl)) // 应用内评分
//: private let apPay            = "apPay"              // 苹果支付
private let data_packageFormat = (String(userSourceUrl.suffix(5))) // 苹果支付
//: private let subscribe        = "subscribe"          // 苹果支付-订阅
private let constPushText = (constAppId.replacingOccurrences(of: "after", with: "b") + String(const_responseMsg)) // 苹果支付-订阅
//: private let openSystemBrowser = "openSystemBrowser" // 调用系统浏览器打开url
private let noti_rootData = (String(userMethodKey.suffix(6)) + "stemBrow" + String(mainRunData)) // 调用系统浏览器打开url
//: private let closeWebview     = "closeWebview"       // 关闭当前webview
private let user_packageTitle = (String(mainStateUrl)) // 关闭当前webview
//: private let openNewWebview   = "openNewWebview"     // 使用新webview打开url
private let userPageMessage = (dataDeviceContent.replacingOccurrences(of: "remove", with: "pe") + String(main_sceneKey.suffix(6)) + "view") // 使用新webview打开url
//: private let reloadWebview    = "reloadWebview"      // 重载webView
private let data_objectPushMessage = (userMicTitle.lowercased() + String(noti_largePanelContent.suffix(8))) // 重载webView

//: struct JSMessageModel: HandyJSON {
struct AttributeHandyJSON: HandyJSON {
    //: var typeName = ""
    var typeName = ""
    //: var token: String?
    var token: String?
    //: var totalCount: Double?
    var totalCount: Double?

    //: var showText: String?
    var showText: String?
    //: var data: UserInfoModel?
    var data: TandemHandyJSON?
    // 内购/订阅 H5参数
    //: var goodsId: String?
    var goodsId: String? // 商品Id
    //: var source: Int?
    var source: Int? // 充值来源
    //: var type: Int?
    var type: Int? // 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    //: var url: String?
    var url: String? // url
    //: var fullscreen: Int?
    var fullscreen: Int? // fullscreen：0:页面从状态栏以下渲染 1:全屏
    //: var transparency: Int?
    var transparency: Int? // transparency：0-webview白色背景 1-webview透明背景
}

//: struct UserInfoModel: HandyJSON {
struct TandemHandyJSON: HandyJSON {
    //: var headPic: String?
    var headPic: String? // 头像
    //: var nickname: String?
    var nickname: String? // 昵称
    //: var uid: String?
    var uid: String? // 用户Id
}

//: extension AppWebViewController {
extension OperationPhaseTop {
    //: func handleH5Message(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
    func product(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
        //: if let model = JSMessageModel.deserialize(from: schemeDic) {
        if let model = AttributeHandyJSON.deserialize(from: schemeDic) {
            //: switch model.typeName {
            switch model.typeName {
            //: case getDeviceID:
            case dataTitleViewId:
                //: let adidStr = AppAdjustManager.getAdjustAdid()
                let adidStr = ScreenWithoutState.pushAdid()
                //: callBack(["typeName": model.typeName, "deviceID": adidStr])
                callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName, String(bytes: const_logName.map{statuteTitle(operation: $0)}, encoding: .utf8)!: adidStr])

            //: case getFirebaseID:
            case k_objectOperationMessage:
                //: AppWebViewController.getFireBaseToken { str in
                OperationPhaseTop.path { str in
                    //: callBack(["typeName": model.typeName, "fireBaseID": str])
                    callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName, String(bytes: data_cornerAlbumFormat.map{spaceLine(black: $0)}, encoding: .utf8)!: str])
                }

            //: case getAreaISO:
            case notiNetKey:
                //: let arr = AppWebViewController.getCountryISOCode()
                let arr = OperationPhaseTop.access()
                //: callBack(["typeName": model.typeName, "areaISO": arr.joined(separator: ",")])
                callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName, String(bytes: noti_logSharedPath.map{$0^88}, encoding: .utf8)!: arr.joined(separator: ",")])

            //: case getProxyStatus:
            case constGroupWarnMessage:
                //: let status = AppWebViewController.getUsedProxyStatus()
                let status = OperationPhaseTop.after()
                //: callBack(["typeName": model.typeName, "isProxy": status])
                callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName, String(bytes: noti_environmentPath.map{titleTrackBody(point: $0)}, encoding: .utf8)!: status])

            //: case getLangCode:
            case noti_zoneLineMsg:
                //: let langCode = UIDevice.langCode
                let langCode = UIDevice.langCode
                //: callBack(["typeName": model.typeName, "langCode": langCode])
                callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName, String(bytes: user_sessionRegionValue.map{$0^48}, encoding: .utf8)!: langCode])

            //: case getTimeZone:
            case kInstallTotalryContent:
                //: let timeZone = UIDevice.timeZone
                let timeZone = UIDevice.timeZone
                //: callBack(["typeName": model.typeName, "timeZone": timeZone])
                callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName, String(bytes: show_decideTitle.map{sizeInfo(tap: $0)}, encoding: .utf8)!: timeZone])

            //: case getInstalledApps:
            case k_decisionDeviceKey:
                //: let apps = UIDevice.getInstalledApps
                let apps = UIDevice.getInstalledApps
                //: callBack(["typeName": model.typeName, "installedApps": apps])
                callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName, String(bytes: app_bridgeRevenueKey.map{toNumber(permission: $0)}, encoding: .utf8)!: apps])

            //: case getSystemUUID:
            case main_systemId:
                //: let uuid = UIDevice.systemUUID
                let uuid = UIDevice.systemUUID
                //: callBack(["typeName": model.typeName, "systemUUID": uuid])
                callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName, String(bytes: app_reduceValue.map{$0^117}, encoding: .utf8)!: uuid])

            //: case getCountryCode:
            case app_entityServerKey:
                //: let countryCode = UIDevice.countryCode
                let countryCode = UIDevice.countryCode
                //: callBack(["typeName": model.typeName, "countryCode": countryCode])
                callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName, String(bytes: showIndexLinkToStr.map{presentationRatingActive(title: $0)}, encoding: .utf8)!: countryCode])

            //: case inAppRating:
            case noti_descriptionPath:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.requestInAppRating()
                OperationPhaseTop.adjustRating()

            //: case apPay:
            case data_packageFormat:
                //: if let goodsId = model.goodsId, let source = model.source {
                if let goodsId = model.goodsId, let source = model.source {
                    //: self.applePay(productId: goodsId, source: source, payType: .Pay) { success in
                    self.offHandCompletion(productId: goodsId, source: source, payType: .Pay) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName, String(bytes: const_forwardUrl.map{$0^90}, encoding: .utf8)!: success])
                    }
                }

            //: case subscribe:
            case constPushText:
                //: if let goodsId = model.goodsId {
                if let goodsId = model.goodsId {
                    //: self.applePay(productId: goodsId, payType: .Subscribe) { success in
                    self.offHandCompletion(productId: goodsId, payType: .Subscribe) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName, String(bytes: const_forwardUrl.map{$0^90}, encoding: .utf8)!: success])
                    }
                }

            //: case openSystemBrowser:
            case noti_rootData:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url, let openURL = URL(string: urlStr) {
                if let urlStr = model.url, let openURL = URL(string: urlStr) {
                    //: UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                    UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                }

            //: case closeWebview:
            case user_packageTitle:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName])
                //: self.closeWeb()
                self.enableHauled()

            //: case openNewWebview:
            case userPageMessage:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url,
                if let urlStr = model.url,
                   //: let transparency = model.transparency,
                   let transparency = model.transparency,
                   //: let fullscreen = model.fullscreen {
                   let fullscreen = model.fullscreen
                {
                    //: AppWebViewController.openNewWebView(urlStr, transparency, fullscreen)
                    OperationPhaseTop.vane(urlStr, transparency, fullscreen)
                }

            //: case reloadWebview:
            case data_objectPushMessage:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName])
                //: self.reloadWebView()
                self.global()

            //: case getMicStatus:
            case showDismissKey:
                //: AppPermissionTool.shared.requestMicPermission { auth, isFirst in
                SecondGridOrientation.shared.currentObject { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName, String(bytes: noti_takeTitle.map{$0^143}, encoding: .utf8)!: auth, String(bytes: userCanArrayStr.reversed(), encoding: .utf8)!: isFirst])
                }

            //: case getPhotoStatus:
            case constMonitorText:
                //: AppPermissionTool.shared.requestPhotoPermission { auth, isFirst in
                SecondGridOrientation.shared.handle { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName, String(bytes: noti_takeTitle.map{$0^143}, encoding: .utf8)!: auth, String(bytes: userCanArrayStr.reversed(), encoding: .utf8)!: isFirst])
                }

            //: case getCameraStatus:
            case noti_pushPhotoStr:
                //: AppPermissionTool.shared.requestCameraPermission { auth, isFirst in
                SecondGridOrientation.shared.head { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName, String(bytes: noti_takeTitle.map{$0^143}, encoding: .utf8)!: auth, String(bytes: userCanArrayStr.reversed(), encoding: .utf8)!: isFirst])
                }

            //: case reportAdjust:
            case userPlusName:
                //: if let token = model.token {
                if let token = model.token {
                    //: if let count = model.totalCount {
                    if let count = model.totalCount {
                        //: AppAdjustManager.addPurchasedEvent(token: token, count: count)
                        ScreenWithoutState.takeResponse(token: token, count: count)
                        //: } else {
                    } else {
                        //: AppAdjustManager.addEvent(token: token)
                        ScreenWithoutState.go(token: token)
                    }
                }
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName])

            //: case requestLocalPush:
            case constMaxKey:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: appNetworkUrl.map{trackFlexible(name: $0)}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.pushLocalNotification(model)
                OperationPhaseTop.instance(model)

            //: default: break
            default: break
            }
        }
    }
}

// MARK: - Event

//: extension AppWebViewController {
extension OperationPhaseTop {
    /// 打开新的webview
    /// - Parameters:
    ///   - urlStr: web地址
    ///   - transparency: 0：白色背景 1：透明背景
    ///   - fullscreen: 0:页面从状态栏以下渲染  1：全屏
    //: class func openNewWebView(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
    class func vane(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
        //: let vc = AppWebViewController()
        let vc = OperationPhaseTop()
        //: vc.urlString = urlStr
        vc.urlString = urlStr
        //: vc.clearBgColor = (transparency == 1)
        vc.clearBgColor = (transparency == 1)
        //: vc.fullscreen = (fullscreen == 1)
        vc.fullscreen = (fullscreen == 1)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: AppConfig.currentViewController()?.present(vc, animated: true)
        LayoutAroundActivity.afoot()?.present(vc, animated: true)
    }

    /// 本地推送
    //: class func pushLocalNotification(_ model: JSMessageModel) {
    class func instance(_ model: AttributeHandyJSON) {
        //: guard UIApplication.shared.applicationState != .active else { return }
        guard UIApplication.shared.applicationState != .active else { return }
        //: UNUserNotificationCenter.current().getNotificationSettings { setting in
        UNUserNotificationCenter.current().getNotificationSettings { setting in
            //: switch setting.authorizationStatus {
            switch setting.authorizationStatus {
            //: case .notDetermined, .denied, .ephemeral:
            case .notDetermined, .denied, .ephemeral:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: print()
                print()

            //: case .provisional, .authorized:
            case .provisional, .authorized:
                //: if let dataModel = model.data {
                if let dataModel = model.data {
                    //: let content = UNMutableNotificationContent()
                    let content = UNMutableNotificationContent()
                    //: content.title = dataModel.nickname ?? ""
                    content.title = dataModel.nickname ?? ""
                    //: content.body = model.showText ?? ""
                    content.body = model.showText ?? ""
                    //: let identifier = dataModel.uid ?? "\(AppName)__LocalPush"
                    let identifier = dataModel.uid ?? "\(app_maxTotalFormat)" + (String(kPlatformPath.suffix(9)) + main_panelUpgradeKey.replacingOccurrences(of: "date", with: "s"))
                    //: content.userInfo = ["identifier": identifier]
                    content.userInfo = [String(bytes: k_mirrorUrl.map{$0^142}, encoding: .utf8)!: identifier]
                    //: content.sound = UNNotificationSound.default
                    content.sound = UNNotificationSound.default

                    //: let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    //: let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    //: let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    //: UNUserNotificationCenter.current().add(request) { _ in
                    UNUserNotificationCenter.current().add(request) { _ in
                    }
                }

            //: @unknown default:
            @unknown default:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: break
                break
            }
        }
    }

    /// 获取firebase token
    //: class func getFireBaseToken(tokenBlock: @escaping (_ str: String) -> Void) {
    class func path(tokenBlock: @escaping (_ str: String) -> Void) {
        //: Messaging.messaging().token { token, _ in
        Messaging.messaging().token { token, _ in
            //: if let token = token {
            if let token = token {
                //: tokenBlock(token)
                tokenBlock(token)
                //: } else {
            } else {
                //: tokenBlock("")
                tokenBlock("")
            }
        }
    }

    /// 获取ISO国家代码
    //: class func getCountryISOCode() -> [String] {
    class func access() -> [String] {
        //: var tempArr: [String] = []
        var tempArr: [String] = []
        //: let info = CTTelephonyNetworkInfo()
        let info = CTTelephonyNetworkInfo()
        //: if let carrierDic = info.serviceSubscriberCellularProviders {
        if let carrierDic = info.serviceSubscriberCellularProviders {
            //: if !carrierDic.isEmpty {
            if !carrierDic.isEmpty {
                //: for carrier in carrierDic.values {
                for carrier in carrierDic.values {
                    //: if let iso = carrier.isoCountryCode, !iso.isEmpty {
                    if let iso = carrier.isoCountryCode, !iso.isEmpty {
                        //: tempArr.append(iso)
                        tempArr.append(iso)
                    }
                }
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 获取用户代理状态
    //: class func getUsedProxyStatus() -> Bool {
    class func after() -> Bool {
        //: if AppWebViewController.isUsedProxy() || AppWebViewController.isUsedVPN() {
        if OperationPhaseTop.country() || OperationPhaseTop.modelValue() {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedProxy() -> Bool {
    class func country() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }

        //: if let httpProxy = dict["HTTPProxy"] as? String, !httpProxy.isEmpty { return true }
        if let httpProxy = dict[(String(constWithFileId) + String(noti_presentationId))] as? String, !httpProxy.isEmpty { return true }
        //: if let httpsProxy = dict["HTTPSProxy"] as? String, !httpsProxy.isEmpty { return true }
        if let httpsProxy = dict[(show_currentMsg.replacingOccurrences(of: "poor", with: "S") + String(show_versionTitle))] as? String, !httpsProxy.isEmpty { return true }
        //: if let socksProxy = dict["SOCKSProxy"] as? String, !socksProxy.isEmpty { return true }
        if let socksProxy = dict[(user_countryData.replacingOccurrences(of: "device", with: "K") + "SProx" + String(showUsId))] as? String, !socksProxy.isEmpty { return true }

        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedVPN() -> Bool {
    class func modelValue() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(show_grantStr.prefix(5)) + "PED__")] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((data_fromProgressFormat.replacingOccurrences(of: "vertical", with: "t"))) || keyStr.contains((showPageStr.replacingOccurrences(of: "create", with: "t"))) || keyStr.contains((app_selectedMsg.lowercased())) || keyStr.contains((app_modelPath.lowercased())) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }

    /// 请求应用内评分 - iOS 13+ 优化版本
    //: class func requestInAppRating() {
    class func adjustRating() {
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            // iOS 14+ 使用新的 WindowScene API（推荐）
            //: if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
            if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
                //: SKStoreReviewController.requestReview(in: windowScene)
                SKStoreReviewController.requestReview(in: windowScene)
            }
            //: } else {
        } else {
            // iOS 13.x 使用传统 API
            //: SKStoreReviewController.requestReview()
            SKStoreReviewController.requestReview()
        }
    }

    // MARK: - Event

    func appCaptureRating() {
        OffsetOperationInteraction.first((String(k_protectionValue.suffix(4)) + "y Af" + String(appCaptureRatingData.suffix(8)) + String(k_topTitle)) + "\"" + (String(user_warnClickRequestName.suffix(7)) + k_sinceTitle.lowercased()) + "\"" + (String(dataArrayName.prefix(4)) + "contact" + String(dataMessageCoreMsg)))
    }
    
    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: func applePay(productId: String, source: Int = -1, payType: ApplePayType, completion: ((Bool) -> Void)? = nil) {
    func offHandCompletion(productId: String, source: Int = -1, payType: ActionType, completion: ((Bool) -> Void)? = nil) {
        //: ProgressHUD.show()
        OffsetOperationInteraction.field()
        //: var index = 0
        var index = 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, _, _ in
        MalusPumilaTransactionObserver.shared.serverList(productId: productId, payType: payType, source: index) { status, _, _ in
            //: ProgressHUD.dismiss()
            OffsetOperationInteraction.macroscopical()
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: var isSuccess = false
                var isSuccess = false
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us")
                    self.appCaptureRating()
                //: case .veritySucceed, .renewSucceed:
                case .veritySucceed, .renewSucceed:
                    //: isSuccess = true
                    isSuccess = true
                    //: self.third_jsEvent_refreshCoin()
                    self.of()

                //: default:
                default:
                    //: print(" apple支付充值失败：\(status.rawValue)")
                    //: break
                    break
                }
                //: completion?(isSuccess)
                completion?(isSuccess)
            }
        }
    }
}
